// const db = require("../../db/db");

// // select  word
// function selecWord(language) {
//   return db.query("select english, german from words where id = $1;", [id]);
// }

// // create image from api
// function addImage(id) {
//   return db.query("insert into words set image_url = $1;", [id]);
// }
